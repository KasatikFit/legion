<?php
/**
 * Секретные токены для ссылок на отчёты.
 * Скопируйте в report_token_config.php на сервере (не в Git).
 *
 * ССЫЛКА (любая группа):
 *   /report.php?token=ВАШ_ТОКЕН&coach=yakutin&name=Фамилия%20Имя
 *
 * ПИЛОТ:
 *   /pilot-demo/report.php?token=ВАШ_ТОКЕН&name=Фамилия%20Имя
 *
 * ВАЖНО:
 * - Строка с токеном НЕ должна быть закомментирована (без //)
 * - Токен с символом $ — только в ОДИНАРНЫХ кавычках '...'
 * - В ссылке и в этом файле — один и тот же токен
 */
return array(
    'global' => 'ВСТАВЬТЕ_СЮДА_ВАШ_ТОКЕН',
);
